[x-axis]
where = top

[hic matrix]
file = K562_3reps_corrected.h5
title = Hi-C data threshold 0.05
colormap= Spectral_r
# depth is the maximum distance plotted in bp. In Hi-C tracks
# the height of the track is calculated based on the depth such
# that the matrix does not look deformed
depth = 8000000
transform = log1p
file_type = hic_matrix

[tads]
file = K562_3reps_TAD_step1500_min3000_max31500_thres0.05_delta0.01_fdr_domains.bed 
file_type = domains
border_color = black
color = none
# the tads are overlay over the hic-matrix
# the share-y options sets the y-axis to be shared
# between the Hi-C matrix and the TADs.
overlay_previous = share-y

[hic matrix]
file = K562_3reps_corrected.h5
title = Hi-C data threshold 0.01
colormap= Spectral_r
# depth is the maximum distance plotted in bp. In Hi-C tracks
# the height of the track is calculated based on the depth such
# that the matrix does not look deformed
depth = 8000000
transform = log1p
file_type = hic_matrix

[tads]
file = K562_3reps_TAD_step1500_min10000_max40000_thres0.01_delta0.01_fdr_domains.bed
file_type = domains
border_color = black
color = none
# the tads are overlay over the hic-matrix
# the share-y options sets the y-axis to be shared
# between the Hi-C matrix and the TADs.
overlay_previous = share-y

[hic matrix]
file = K562_3reps_corrected.h5
title = Hi-C data threshold 0.005
colormap= Spectral_r
# depth is the maximum distance plotted in bp. In Hi-C tracks
# the height of the track is calculated based on the depth such
# that the matrix does not look deformed
depth = 8000000
transform = log1p
file_type = hic_matrix

[tads]
file = K562_3reps_TAD_step1500_min10000_max40000_thres0.005_delta0.01_fdr_domains.bed
file_type = domains
border_color = black
color = none
# the tads are overlay over the hic-matrix
# the share-y options sets the y-axis to be shared
# between the Hi-C matrix and the TADs.
overlay_previous = share-y

[hic matrix]
file = K562_3reps_corrected.h5
title = Hi-C data threshold 0.001
colormap= Spectral_r
# depth is the maximum distance plotted in bp. In Hi-C tracks
# the height of the track is calculated based on the depth such
# that the matrix does not look deformed
depth = 8000000
transform = log1p
file_type = hic_matrix

[tads]
file = K562_3reps_TAD_step1500_min10000_max40000_thres0.001_delta0.01_fdr_domains.bed
file_type = domains
border_color = black
color = none
# the tads are overlay over the hic-matrix
# the share-y options sets the y-axis to be shared
# between the Hi-C matrix and the TADs.
overlay_previous = share-y

[spacer]

[bedgraph matrix file test]
file = K562_3reps_TAD_tad_score.bm 
height = 4
type = lines
pos_score_in_bin = center
show_data_range = true
title = TAD separation score
plot_horizontal_lines = false
file_type = bedgraph_matrix

[gene track]
file = hg19_genes.bed
height = 10
title = hg19 genes
labels = false
